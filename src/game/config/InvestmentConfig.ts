/** Global passive-income payout cadence for all owned investments. */
export const InvestmentConfig = {
  /** How often owned investments pay out (milliseconds). */
  payoutIntervalMs: 5000,
} as const
