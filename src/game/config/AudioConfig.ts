/** Default levels and persistence key for audio settings. */
export const AudioConfig = {
  defaultMusicVolume: 0.45,
  defaultSfxVolume: 1,
  settingsStorageKey: 'horse-fishing-game:audio-settings:v1',
} as const
